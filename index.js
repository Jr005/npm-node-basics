// Automatically it will look in the node modules
// folders so code is needed to look in the correct file

// var variables = require("./variables.js");

// short version
var v = require("./variables.js");

// console.log(variables);

// short version
console.log(v.name);

// Print Function
console.log(v.printName("Brandon"));
